import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "sang-frontend-ionic",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
